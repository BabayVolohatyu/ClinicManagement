@model ClinicManagement.Models.Facilities.CabinetType

@{
    ViewData["Title"] = "Cabinet Type Details";
}

<h2>Cabinet Type Details</h2>

<form asp-action="Update" asp-route-id="@Model.Id" method="post" id="editForm">
    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>ID</th>
                <td>
                    <input asp-for="Id" class="form-control-plaintext" readonly />
                </td>
            </tr>
            <tr onclick="makeEditable(this)">
                <th>Type</th>
                <td>
                    <input asp-for="Type" class="form-control-plaintext" readonly />
                    <span asp-validation-for="Type" class="text-danger"></span>
                </td>
            </tr>
        </tbody>
    </table>

    <div class="mt-3">
        <button type="submit" class="btn btn-primary">Save</button>
        <button type="submit" form="deleteForm" class="btn btn-danger">Delete</button>
        <a asp-action="Index" class="btn btn-secondary">Back</a>
    </div>
</form>

<form id="deleteForm" asp-action="Delete" asp-route-id="@Model.Id" method="post" style="display: none">
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        function makeEditable(row) {
            const input = row.querySelector("input");
            if (input && input.name !== "Id" && input.hasAttribute("readonly")) {
                input.removeAttribute("readonly");
                input.classList.remove("form-control-plaintext");
                input.classList.add("form-control");
            }
        }
    </script>
}
